# BACKLOG.md

Aufgabenliste für SubTracked. Reihenfolge = grobe Priorität. Erledigtes abhaken, nicht löschen (Verlauf).

## ✅ Erledigt

- [x] Tauri-v2-Gerüst (React + TypeScript, pnpm)
- [x] Plugins eingebunden: `sql`, `notification`, `autostart`
- [x] SQLite-Schema als Migration (`0001_init.sql`) + in `lib.rs` registriert
- [x] `sqlite`-Feature in `Cargo.toml` aktiviert
- [x] Framework-unabhängige Logik vorhanden: `recurrence.ts`, `coverage.ts`, `db.ts`, `reminders.ts`
- [x] Auf GitHub gepusht (Commit mit Schutz-E-Mail)

## 🐛 Bugs

- [x] **Dropdown-Lesbarkeit im Dark-Mode** (2026-06-06) — gefixt mit `color-scheme: dark` auf `:root` im Dark-Mode-Media-Query. Erster Versuch über `select option { color/background }` wirkungslos, weil WebKitGTK das Option-Popup nativ über das OS-Theme rendert und CSS-Styling auf `<option>`-Elementen ignoriert. `color-scheme: dark` ist das standardisierte Signal an den UA, native Form-Controls im dunklen Stil zu zeichnen — bringt nebenbei auch Calendar-Popup und ähnliches mit ins dunkle Theme.
- [x] **Datenpersistenz nach Reboot — verifiziert stabil** (2026-06-06). User hatte am 2026-06-05 berichtet, nach einem System-Neustart seien angelegte Abos weg. Untersuchung am selben Tag (siehe HANDOVER 2026-06-05 — Persistenz-Diagnose) ergab schon damals: DB unter `~/.config/com.tcgtvv.subtracked/subtracker.db` enthält die Daten, WAL aktiv, Identifier nie geändert, Code-Pfad clean. Nach der Architektur-➌-Foundation am 2026-06-06 (eigener sqlx-Pool mit explizitem `journal_mode=Wal` + `create_if_missing=true`) noch einmal nach Reboot vom User geprüft → Persistenz weiter gesund. Verdacht endgültig vom Tisch.

  Falls das Phänomen wieder auftritt, in dieser Reihenfolge:
  1. `ls -la ~/.config/com.tcgtvv.subtracked/` — DB + WAL + SHM da, Zeitstempel sinnvoll?
  2. `sqlite3 ~/.config/com.tcgtvv.subtracked/subtracker.db "SELECT * FROM subscriptions;"` — Einträge in der DB?
  3. Wenn DB voll, UI leer → Frontend-Bug. Devtools-Console im `pnpm tauri dev` prüfen (silent rejection in `listSubscriptions`?).
  4. Wenn DB leer → Connection-Lifecycle in `tauri-plugin-sql` auditieren; PRAGMA `journal_mode`/`synchronous`; prüfen ob das Verzeichnis von außen geleert wird.

  **DB-Pfad-Sonderheit**: `tauri-plugin-sql` nutzt `app_config_dir()`, nicht `app_data_dir()` — die Datei landet daher im Config-Dir (`~/.config/...` auf Linux), obwohl es Nutzdaten sind. Footgun für Suchen am falschen Ort.
- [x] **Aus dem System Tray heraus kann man das Fenster nicht aufpoppen lassen** (2026-06-07, Codex) — gefixt auf **CachyOS + KDE Plasma**. `show_main_window` macht jetzt `show()`/`unminimize()` und setzt den Fokus verzögert; auf Linux wird zusätzlich kurz `always_on_top` getoggelt, um KWin/Plasma einen Raise-Impuls zu geben. User hat den Dev-Build manuell getestet und bestätigt: Fenster kommt aus dem Tray wieder hoch; App anschließend manuell beendet.
- [x] **Reminder darf nicht als "gesendet" gelten, wenn keine Notification sichtbar wurde** (2026-06-07, Codex) — `reminders.rs` schreibt den Reminder-Row jetzt erst nach erfolgreichem `show()`-Aufruf. Bei fehlender Notification-Permission wird kein Row geschrieben, sondern ein `tracing::info!` mit `subscription_id` und `due_date` geloggt; der Check kann nach spaeterer Permission erneut versuchen. Vorher wurde der Idempotenz-Row vor bzw. unabhaengig vom Versand geschrieben.
- [x] **KRW/Zero-Decimal-Währungen in Rust-Notifications korrekt formatieren** (2026-06-07, Codex) — Rust-Notifications nutzen jetzt eine kleine Subdivision-/Format-Logik analog zum Frontend: EUR/USD/etc. als `17,99 EUR`, KRW ohne `/100` als `1.500 KRW`. Tests decken Regular Currency, KRW und defensive negative Werte ab.
- [x] **Alten `insert_reminder_if_new`-Command entfernen/entschaerfen** (Review/Fix 2026-06-08, Codex). Der oeffentliche Tauri-Command und der TS-Wrapper sind entfernt; Reminder-Rows koennen damit nicht mehr versehentlich aus dem Frontend als "gesendet" markiert werden. Der Scheduler-interne Helper in `reminders.rs` bleibt erhalten.
- [x] **Reminder-Status im Settings-Dialog beim Oeffnen frisch laden** (Review/Fix 2026-06-08, Codex). `App.tsx` gibt einen `openSeq` an den dauerhaft gemounteten `SettingsDialog`; der Dialog laedt den Reminder-Status beim Mount und bei jedem Oeffnen erneut.
- [x] **compute_due_reminders bricht den ganzen Batch bei einer korrupten Zeile ab** (2026-06-08, Review; gefixt 2026-06-08, Codex) — `compute_due_reminders` loggt kaputte `anchor_date`-/Intervall-Zeilen jetzt pro Abo mit `tracing::warn!` und skippt sie, statt den ganzen Tick per `?` abzubrechen. Test deckt gemischten Batch mit kaputter und gueltiger Zeile ab.
- [x] **Shutdown-Race: notification.show() vor INSERT, kein Cancel-Signal** (2026-06-08, Review; gefixt 2026-06-08, Codex) — Dispatcher reserviert die `(subscription_id, due_date)`-Zeile jetzt direkt vor `show()` via `INSERT OR IGNORE`; bei `show()`-Fehler wird die Reservierung wieder geloescht. Damit fuehrt ein Shutdown nach OS-Notification nicht mehr zur naechsten Doppelbenachrichtigung.
- [x] **balance_cents ungeprueft in add_account/update_account** (2026-06-08, Review; gefixt 2026-06-08, Codex) — `validate_account_fields` validiert `balance_cents` jetzt als vierten Parameter mit weichem Safe-Integer-Range-Check (`±9_000_000_000_000_000` kleinste Waehrungseinheiten); negative Kontosalden bleiben erlaubt. Tests ergaenzt.
- [x] **Orphan account_id blockiert Edit der Sub-Row** (2026-06-08, Review; gefixt 2026-06-08, Codex) — `update_subscription` validiert eine konkrete `account_id` nur noch, wenn sie sich gegenueber dem gespeicherten Wert aendert. Unveraenderte Legacy-Orphans und Wechsel auf `null` blockieren das Speichern nicht mehr; Wechsel auf eine neue ID bleibt per `validate_account_exists` geschuetzt. Pure Tests decken unchanged/changed/cleared ab.
- [x] **Legacy lead_days bei Read nicht re-validiert** (2026-06-08, Review; gefixt 2026-06-08, Codex) — `compute_due_reminders` prueft `lead_days` jetzt pro Abo mit `validate_lead_days`; negative oder >365 werden mit `tracing::warn!` geskippt, statt das Vorlauffenster kaputtzurechnen. Test deckt gemischten Batch mit ungueltigen und gueltigen Werten ab.
- [x] **Anchor-Date strict-on-write, lenient-on-read** (2026-06-08, Review; gefixt 2026-06-08, Codex) — Neuer gemeinsamer `parse_iso_date_strict`-Helper in `recurrence.rs`; `validate_anchor_date` und `compute_due_reminders` nutzen denselben strikten `YYYY-MM-DD`-Parser. Tests decken unpadded Legacy-Datum im Read-Pfad ab.

## 🔨 Jetzt (Oberfläche)

- [x] **Abo-Liste** anzeigen (Name, Betrag, nächste Fälligkeit, Konto) — nutzt `db.listSubscriptions()` + `recurrence.nextDueDate()`
- [x] **Abo anlegen / bearbeiten / löschen** (Formular) — `db.addSubscription()`, `db.updateSubscription()`, `db.deleteSubscription()`
- [x] **Konten** anlegen/auswählen — `db.listAccounts()`, `db.addAccount()`, `db.deleteAccount()` mit Soft-Check (`countSubsForAccount()`)
- [x] **Kontodeckungs-Ansicht** (anstehende Abflüsse je Konto, N Monate) — `coverage.computeCoverage()`, collapsible pro Konto
- [x] **Monatliche Fixkosten-Übersicht** ("Baseline") — `coverage.computeMonthlyBaseline()`, pro Konto + Gesamt-Summe
- [x] **Erinnerungs-Check verdrahten:** `runReminderCheck()` beim App-Start + stündliches Intervall in `App.tsx`
- [x] Standard-Template-Reste (`greet`-Demo) durch echte UI ersetzen
- [x] **Sichtbare Formular-Validierung im Abo-Dialog** (2026-06-07, Claude) — `SubscriptionDialog` validiert beim Submit Name/Betrag/Vorlauf, zeigt feldnahe `field-error`-Spans mit `role="alert"`, setzt `aria-invalid` + `aria-describedby` und fokussiert das erste fehlerhafte Feld in DOM-Reihenfolge. Tippen im Feld räumt den jeweiligen Fehler weg. Zur Konsistenz hat `AccountsDialog` dieselbe Behandlung bekommen — Validierungs-Logik in `validate()`-Helper, drei `useRef`-Anker für Fokus, geteilte `.field-error`-CSS mit Dark-Mode-Anpassung.
- [x] **Abo archivieren/pausieren statt nur löschen** (2026-06-07, Claude) — Schlanker Rust-Command `set_subscription_active(id, active)` (kein Full-Update), Wrapper `setSubscriptionActive` in `db.ts`. `useSubscriptions` lädt jetzt alle Subs (`listSubscriptions(false)`); `App.tsx` splittet selbst und übergibt nur `activeSubs` an `OverviewSection`, damit Coverage keine Phantom-Abflüsse archivierter Abos prognostiziert. Pro Abo neuer Knopf "Archivieren"/"Reaktivieren" zusätzlich zu Löschen. Checkbox-Toggle "Archivierte anzeigen (N Abos)" wird nur eingeblendet, wenn es mindestens ein archiviertes Abo gibt. Archivierte Items sind ausgegraut (`.sub-archived` mit `opacity: 0.6` + dezentem Hintergrund) und zeigen "archiviert" statt nächste Fälligkeit. Stummschaltung wird bei archivierten ausgeblendet (irrelevant, weil Reminder-Loop bereits per `WHERE active = 1` filtert).
- [x] **Nächste-Fälligkeiten-Ansicht als primärer Arbeitsmodus** (2026-06-07, Claude) — Neue `computeUpcoming(subs, accounts, days = 30)` in [coverage.ts](src/lib/coverage.ts) als reine Funktion, neue Komponente [UpcomingSection.tsx](src/components/UpcomingSection.tsx) wird in `App.tsx` direkt **über** der Abo-Liste gerendert (sobald `activeSubs.length > 0`). Pro Zeile: Datum `dd.MM.` · Name · Konto (oder `(kein Konto)`) · Betrag. Stumme Abos bekommen `· stumm` als Suffix am Namen. Quellenmenge: nur aktive Subs (archivierte ignorieren — Aufrufer-Verantwortung, in `computeUpcoming`-Doc explizit erklärt). 10 neue Tests (5 für `computeUpcoming` in coverage.test.ts, 5 für `UpcomingSection`). Keine Klick-Aktion erstmal — Bearbeiten/Archivieren/Löschen bleiben in der Abo-Liste darunter, kann später als „direkt von hier editieren"-Komfort dazukommen.
- [x] **Filter und Sortierung für Abo-Liste** (2026-06-07, Claude) — Neue `applyFilterAndSort`-Pure-Function in [subscription-list.ts](src/lib/subscription-list.ts) mit 17 Tests. Neue `SubscriptionFilterBar`-Komponente rendert vier Selects (Konto/Währung/Erinnerungen/Sortierung) oberhalb der Abo-Liste, **nur wenn ≥ 2 Abos sichtbar sind**. Konto- und Währungs-Filter blenden sich aus, wenn nur eine Option existiert (kein UI ohne Funktion). 6 Sortier-Optionen: Name A↔Z, Fälligkeit (nächste/späteste zuerst), Betrag auf/ab. Empty-State „Kein Abo passt zu den aktuellen Filtern." wenn alle rausgefiltert werden. Aktiv/Archiviert bewusst **nicht** im neuen Filter — der bestehende Archiv-Toggle deckt das ab (ein Konzept pro UI-Pfad). Filter-Bar arbeitet auf den vom Archiv-Toggle preFiltered Subs, sodass beide Konzepte sauber kombinierbar bleiben. Fälligkeits-Sortierung iteriert nur über sichtbare Subs und cacht `nextDueDate` pro Sub.
- [x] **Backend-Validierungs-Errors als Feld-Errors im Dialog** (2026-06-08, Review; gefixt 2026-06-08, Codex) — Frontend-Validatoren pruefen jetzt striktes `YYYY-MM-DD` und Currency-Whitelist vor dem DB-Aufruf. `SubscriptionDialog` zeigt feldnahe Fehler fuer `anchorDate` und `currency`, `AccountsDialog` fuer `currency`; `DateField` rendert invalide Legacy-Datumswerte roh statt zu crashen. Tests decken ungueltiges Legacy-Datum und unbekannte Waehrungen ab.

## ⏭️ Als Nächstes (Hintergrund-Betrieb)

- [x] **Tray-Icon** + Fenster beim Schließen nur verstecken (App läuft weiter)
- [x] **Autostart beim Login** über Einstellungen aktivierbar (`@tauri-apps/plugin-autostart` `enable()`)
- [x] Notification-Berechtigung sauber abfragen und Status anzeigen
- [x] Vorlaufzeit (`lead_days`) pro Abo in der UI editierbar
- [x] **Notifications pro Abo stummschaltbar** — bei bekannten regelmäßigen Abos (z.B. Netflix monatlich) will man keinen Spam. Neue Spalte `notify INTEGER DEFAULT 1` via Migration, UI-Toggle pro Abo, `runReminderCheck` überspringt stumme Abos. Sie bleiben aber sichtbar in Liste/Fixkosten-Übersicht
- [x] **Test-Notification in den Einstellungen** (2026-06-07, Claude) — Neuer Rust-Command `send_test_notification` ruft direkt `app.notification().builder().show()` mit fester Test-Nachricht. Button "Test-Erinnerung senden" im SettingsDialog, danach „✓ Gesendet — siehst du den Toast?"-Bestätigung mit `role="status"`. Existierende `notification:default`-Permission deckt das ab.
- [x] **Reminder-Status sichtbarer machen** (2026-06-07, Claude) — Neuer `ReminderState { last_check_at: Mutex<Option<DateTime<Utc>>> }` als in-memory App-Singleton (bewusst keine Persistenz — Diagnose-State reicht). Reminder-Loop in `lib.rs` aktualisiert nach jedem Durchlauf (auch bei Error). Neuer Command `get_reminder_status` liefert `{ lastCheckAt, intervalSecs, lastSent }` — `lastSent` per SQL-JOIN `reminders × subscriptions` und `ORDER BY sent_at DESC LIMIT 1`. SettingsDialog rendert `<dl>` mit „Intervall / Letzte Prüfung / Nächste Prüfung (= last + intervalSecs) / Letzte Erinnerung", plus „Aktualisieren"-Knopf für Refresh.

## 📈 Produktnutzen / Prognose

- [x] **Kontostände pro Konto erfassen** (2026-06-07, Claude) — Migration `0003_account_balance.sql` ergänzt `accounts.currency` / `balance_cents` / `min_buffer_cents` mit EUR/0/0 als Defaults. `Account`-Struct (Rust + TS) erweitert, neuer `update_account`-Tauri-Command, `AccountsDialog` mit Edit-Mode + Feldern für Währung/Saldo/Mindestpuffer (`type=text + inputMode=decimal + parseAmountInput` wie beim Abo-Betrag, akzeptiert Komma und Tausender).
- [x] **Deckungs-Warnlogik pro Konto** (2026-06-07, Claude) — `computeCoverage` schreibt den Saldo pro Buchung fort (`balanceAfterCents`), merkt sich `firstBelowBufferDate` / `firstBelowZeroDate` und markiert betroffene Buchungen via `belowBuffer` / `belowZero`. OverviewSection rendert pro Konto: Saldo heute → final, plus orange Warnung bei Puffer-Unterschreitung und rote bei Konto-im-Minus. Pro Coverage-Item steht jetzt "→ Saldo danach", farblich markiert wenn kritisch.
- [x] **Mindestpuffer/Zielbetrag je Konto** (2026-06-07, Claude) — als Teil der Deckungs-Warnlogik mit umgesetzt: `accounts.min_buffer_cents` (Default 0), in der UI als optionales Feld "Mindestpuffer". Wenn 0, warnt die App nur bei Negativ-Saldo; bei >0 zusätzlich vor Unterschreitung.
- [x] **Mehrwährungs-Prognose fachlich sauber schneiden** (2026-06-07, Claude) — Konto hat jetzt eine Währung. `computeCoverage` ignoriert Subs in fremder Währung und zählt sie als `foreignCurrencySubsCount`; OverviewSection zeigt den Hinweis "N Abos in anderer Währung werden hier nicht berücksichtigt". `computeMonthlyBaseline` splittet Buckets pro `(Konto, Währung)` statt heimlich zu summieren. Der pauschale `"EUR"`-Hardcode in OverviewSection (Codex-Review-Befund) ist damit weg — `formatAmount` bekommt jetzt die Konto-Währung.
- [ ] **Preisänderungs-Historie pro Abo.** Optionaler Verlauf für Betrag/Währung, damit Preiserhöhungen nachvollziehbar bleiben und alte Zahlungen nicht rückwirkend "umgeschrieben" wirken. Für MVP nicht nötig, aber fachlich passend bei echten Langzeitdaten.

## 🚀 Distribution & Setup

- [x] **Lokales Installer-Build & richtige App-Installation** (2026-06-06) — Smoke-Test auf Cachyos bestanden. Drei Erkenntnisse aus dem Lauf, die direkt in den Code geflossen sind:
  1. **Naming-Fix in `tauri.conf.json`**: `productName: "subtracked"` ergab überall lowercase (Binary, `.desktop`-File, Launcher-Anzeige). Auf `productName: "SubTracked"` + `mainBinaryName: "subtracked"` umgestellt — Display-Name groß (PascalCase), Binary klein (Unix-Konvention). Window-Title ebenfalls auf `SubTracked`.
  2. **WebKit-Wayland-Bug**: Production-Build crashte beim Start mit `Gdk-Message Error 71 (Protokollfehler)` unter Wayland. In `lib.rs` ganz früh als `unsafe { std::env::set_var("WEBKIT_DISABLE_DMABUF_RENDERER", "1") }` mit `#[cfg(target_os = "linux")]`-Gate fest verankert. `pnpm tauri dev` setzt die Var implizit, der Release-Build nicht — deshalb erst beim Smoke-Test sichtbar. Auf X11 no-op, kein Schaden.
  3. **Distribution auf Arch via `debtap`** (lokaler Workaround, kein Production-Pfad): `.deb` → `sudo debtap -Q SubTracked_0.1.0_amd64.deb` → `sudo pacman -U --assume-installed gtk=1 *.pkg.tar.zst`. Das `--assume-installed gtk=1` umgeht debtap's falsches Mapping `libgtk-3-0` → `gtk` (Arch heißt das `gtk3`, das tatsächlich auch separat als Dep gelistet ist). `.AppImage`-Bundling scheiterte lokal an fehlendem `fuse2`-Paket — auf CI-Runnern (siehe Matrix-Build in der Später-Sektion) kein Problem, weil GitHub-Linux-Runner FUSE installiert haben.
  - Geprüft: App startet aus Anwendungsmenü ✓, Tray-Icon ✓, DB unter `~/.config/com.tcgtvv.subtracked/` ✓, App-Eintrag als „SubTracked" sichtbar ✓.
- [ ] **Versions-Tag `v0.1.0`** schneiden, sobald die UI stabil läuft und der Installer-Punkt oben abgenommen ist.
- [ ] **GitHub-Actions-Matrix-Build** (`tauri-action`) für Win/Linux/macOS-Installer bei jedem Release-Tag. Grundlage für v0.1.0 und Voraussetzung für einen robusten Updater-Pfad. Sollte signierte Artefakte plus `latest.json` erzeugen.
- [ ] **In-App Update-Mechanismus via `tauri-plugin-updater`** — sinnvoll *ab* v0.1.0, nicht früher. Ohne stabile, signierte Release-Pipeline wird der Updater-Pfad brüchig. Schritte:
  - Plugin einbinden (`pnpm tauri add updater`), Keypair via `pnpm tauri signer generate`, Public Key in `tauri.conf.json`, Private Key sicher als GitHub-Secret verwahren
  - Update-Endpoint auf GitHub-Releases zeigen: `https://github.com/TCGTVV/SubTracked/releases/latest/download/latest.json`. `latest.json` als Asset bei jedem Release hochladen (`tauri-action` macht das automatisch im Matrix-Build, siehe Später-Sektion)
  - UI: „Nach Updates suchen"-Button im Settings-Dialog, plus optional Auto-Check beim App-Start (mit User-Toggle)
  - Verifizieren: alte Version installieren, neue Version releasen, Update durchspielen (Download → Signatur-Check → Install → Auto-Restart)
  - Abhängig vom Matrix-Build (Später-Sektion), weil ohne automatische signierte Builds der Updater-Pfad zu Hand-Arbeit pro Release wird
- [ ] **README-/GitHub-Polish bei v0.1.0**: Screenshot oder kleines GIF einbetten (Tray-Icon, Hauptfenster, Settings-Dialog), README-Funktionen-Sektion bei Bedarf erweitern, ggf. Demo-Video für die GitHub-Release-Page. Logo ist seit 2026-06-05 unter `assets/logo.png` im README, fehlt also nur noch der UI-visueller-Anker — bewusst noch nicht jetzt, weil die UI noch im Wandel ist.
- [ ] **Windows/macOS Smoke-Test vor v0.1.0.** Mindestens: App startet, DB wird am erwarteten OS-Pfad angelegt, Tray-Verhalten, Notification-Permission, Autostart, Fenster-Schließen-versteckt. Linux ist real genutzt; Windows/macOS sind technisch plausibel, aber noch nicht abgenommen.
- [x] **README-Philosophie umformulieren** (2026-06-06) — Tagline drehte das Tray-Verhalten in den Mittelpunkt; das ist Feature, nicht Identität. Neue Tagline stellt die native Desktop-App in den Vordergrund, Tray als leiser Hintergrund-Modus. Anti-SaaS-Linie (lokal, eigene Daten, kein Account) im Untertitel explizit. Tech-Sektion gleich mit-aktualisiert (sqlx-Pool statt `tauri-plugin-sql`, Reminder-Loop in Rust, Test/CI-Werkzeuge). Neuer „Entwicklung"-Block mit den lokalen Lint-/Test-Befehlen.
- [x] **Logo neu exportieren / fixen** (2026-06-06) — Nano Banana hat das Editor-Schachbrettmuster konsequent als Pixel mit-exportiert, auch nach mehreren Re-Export-Versuchen des Users. Statt das Tool weiter zu trietzen: ImageMagick-Filter (`-fuzz 12% -transparent "#cccccc"` + `-transparent "#f7f7f7"`) entfernt das Schachbrett zuverlässig, weil die Logo-Farben (Teal, Orange) weit von den Pattern-Grautönen entfernt liegen. Im selben Aufwasch: `-resize 1200x -strip`. Ergebnis: 2752×1536 / 5,1 MiB → 1200×670 / 146 KiB (≈97 % kleiner) ohne sichtbaren Qualitätsverlust an den Anti-Aliasing-Kanten.
- [ ] **Fallow** (https://github.com/fallow-rs/fallow-skills) — Rust-native Code-Intelligence-Tool für JS/TS, läuft als Agent-Skill und findet unused code, duplication, circular deps, complexity hotspots, architecture drift. **Recherche 2026-06-07: nicht jetzt einbinden.** Bei SubTracked (~3000 Zeilen JS/TS) deckt Biome + `tsc --noUnusedLocals/--noUnusedParameters` + eigener Blick das schon ab, und Serena via LSP findet zirkuläre/dead Imports auch. Sinnvoll ab Schwelle **~20k LOC oder spürbarer Architektur-Drift** — z.B. wenn beim Refactoring nicht mehr offensichtlich ist, welche Komponenten Reference-Edges zueinander haben. Bei v0.5+ Re-Evaluieren oder wenn die Codebase neue Module dazubekommt.
- [x] **Graphify** (https://github.com/safishamsi/graphify) — geprüft 2026-06-07, **nicht passend**. Macht aus dem Repo einen tree-sitter-AST Knowledge-Graph für 5–10× Token-Reduktion. **Redundant zu Serena**, das via TypeScript-LSP semantische Symbol-Suche macht — dasselbe Problem, besser integriert (nutzt den echten TS-Compiler statt tree-sitter-Heuristik). Zwei Indizes für dasselbe wäre Aufwand ohne Nutzen.
- [x] **Ruflo** (https://github.com/ruvnet/ruflo, früher „Claude-Flow") — geprüft 2026-06-07, **nicht passend**. Multi-Agent-Orchestration-Platform mit Swarms/Federation/RAG. Token-Spar-Versprechen ist unter [konkreter Audit-Kritik](https://gist.github.com/roman-rr/ed603b676af019b8740423d2bb8e4bf6) („99% Theater, 1% Real, 5x Token Waste"). Multi-Agent macht Sinn bei riesigen parallelen Tasks (große Monorepos), nicht bei einem Solo-Projekt mit fokussierten Sessions — würde Tokens eher verschwenden statt sparen. Nicht aufnehmen.

## 📐 Tests & Qualität

Strategie am 2026-06-05 mit dem User diskutiert und festgelegt. Stack-Entscheidungen: **Biome** statt ESLint+Prettier (eine Tool, Rust-basiert, vernünftige Defaults), **vitest** für Pure-Logic-Tests jetzt (Komponenten/E2E später, siehe "Später"), **Lefthook** statt husky (leichter), **GitHub Actions** für CI (nicht der Matrix-Build, der bleibt separat). Schritte in Reihenfolge:

- [x] **Schritt 1: Biome eingerichtet** (2026-06-05) — `biome.json` mit Spaces 2 / lineWidth 100, `pnpm lint` und `pnpm lint:fix` als Scripts, initialer Format-Pass über die ganze Codebase.
- [x] **Schritt 2: Rust-Strenge** (2026-06-05) — `cargo clippy --all-targets -- -D warnings` clean ohne Eingriffe; `cargo fmt --check` hatte zwei triviale Abweichungen in `lib.rs` (eine künstlich umgebrochene Zeile + fehlende Final-Newline), per `cargo fmt` korrigiert.
- [x] **Schritt 3: vitest** (2026-06-05) — `vitest@4.1.8` als devDependency, separate `vitest.config.ts` (env `node`, entkoppelt von der Tauri-`vite.config.ts`), Scripts `pnpm test` / `pnpm test:run`. 26 Tests in `src/lib/{recurrence,coverage,format}.test.ts`: zentrale Drift-Tests für die anker-additive Logik (`nextDueDate`/`dueDatesWithin` mit Anker 31.01.), Bucket-/Sortierungs-/Math.round-Verhalten in `coverage`, Locale-tolerante Regex-Matcher für `formatAmount`. `reminders.ts` (mit DB-Mock) auf später vertagt.
- [x] **Schritt 4: Lefthook** (2026-06-05) — `lefthook@2.1.9` als devDependency. `lefthook.yml` mit vier parallelen Jobs (biome, vitest, cargo fmt, cargo clippy), Trockenlauf in 1,5 s grün. `prepare`-Script ruft `lefthook install` beim `pnpm install`, damit jeder Cloner die Hooks automatisch bekommt. `pnpm-workspace.yaml` (`allowBuilds.lefthook: true`) freigibt das postinstall, das die Go-Binary downloadet.
- [x] **Schritt 5: GitHub Actions CI** (2026-06-05) — `.github/workflows/checks.yml` mit einem `checks`-Job auf `ubuntu-latest`. Steps: Tauri-Linux-Deps via apt, pnpm + Node 20 (cached), Rust-Toolchain mit clippy+rustfmt, `Swatinem/rust-cache` für die Cargo-Builds, dann `pnpm lint` → `pnpm test:run` → `cargo fmt --check` → `cargo clippy --all-targets -- -D warnings`. Triggers: push auf `main` + pull_request. Concurrency-Group cancelt veraltete Runs. Damit ist die ganze Tests-/Qualitäts-Strategie abgeschlossen.
- [x] **Zweiter Agent für Review & Tests** (2026-06-08, Claude) — als manuelle Konvention in [AGENTS.md](./AGENTS.md) Abschnitt „Zweite Augen" verankert: nicht-triviale Aenderungen bekommen vor dem Commit `/code-review high`, groessere Bloecke `/code-review ultra`, Triviales ohne Review. Cross-Agent-Pattern (Claude reviewt Codex und umgekehrt) ist die zweite Augen-Praxis; Befunde werden im HANDOVER dokumentiert oder als Backlog-ToDo aufgenommen, statt mit „mache ich spaeter" durchzurutschen. Bewusst keine Infra (CI-Reviewer, scheduled Review-Agent) — passt nicht zur Solo-Fruehphase-Linie aus [feedback_workflow](memory).
- [x] **Serena-Memories aktualisieren** (2026-06-07, Codex) — `.serena/memories/core.md`, `conventions.md` und `task_completion.md` auf aktuellen sqlx/Tauri-Commands-Stand, Rust-Reminder-Scheduler, aktuelle Test-/Lint-Pipeline und Codex-HANDOVER-Konvention gebracht. Vorher standen dort noch `tauri-plugin-sql`, alte Testlage und veraltete Completion-Regeln.
- [ ] **E2E-Tests via Tauri WebDriver vor `v1.0`.** Nicht jetzt als Dauerbrenner, aber rechtzeitig für stabile User-Flows: Abo anlegen, speichern, App neu starten, Daten wieder sichtbar, Settings/Autostart öffnen, Notification-Testpfad. Großer Setup-Aufwand, ROI erst wenn echte Flows stabil bleiben müssen.
- [x] **Reminder-Dispatcher-Reservierung/Rollback automatisiert testen** (2026-06-08, Review/Codex; gefixt 2026-06-09, Claude) — Neue `Notifier`-Trait in [reminders.rs](src-tauri/src/reminders.rs) als Side-Effect-Seam (Production: `AppNotifier(&AppHandle)`); `dispatch_due_reminders` nimmt jetzt `&dyn Notifier` statt `&AppHandle`. 4 Tests mit `MockNotifier` decken die kritische Reihenfolge ab: Success persistiert die Reservierung, show()-Failure rollt sie zurueck, idempotenter Skip bei bereits gesendetem Row, Permission-denied reserviert nichts.
- [x] **Orphan-account_id-Update als DB-/Command-Test absichern** (2026-06-08, Review/Codex; gefixt 2026-06-09, Claude) — Inner-Helper `update_subscription_in_db(&SqlitePool, &Subscription)` aus dem Tauri-Command extrahiert; 4 Tests mit in-memory sqlx-Pool + Migrations decken unchanged/changed/cleared/invalid ab. Im selben Aufwasch ein Production-Bug gefunden + gefixt: sqlx 0.9 aktiviert SQLite-FK per Default, der bisherige Orphan-Fix blockierte unchanged-Orphan-Updates beim UPDATE-Statement, weil `account_id` weiterhin im `SET`-Clause stand. Fix: bei unveraendertem `account_id` die Spalte komplett aus dem `SET` weglassen, sodass SQLite den FK-Check fuer die Legacy-Bindung nicht ausloest.
- [x] **Recurrence-Vektoren decken non-31-Clamps nicht ab** (2026-06-08, Review; gefixt 2026-06-09, Claude) — Vier neue Vektoren in [tests/fixtures/recurrence-vectors.json](tests/fixtures/recurrence-vectors.json): drei quarterly-Drift-Beweise mit Anker 2024-08-31 (Clamp auf Nov-30 / Feb-28 / Drift-frei zurueck auf May-31) und ein yearly-Schaltjahr-Drift-Beweis (Anker 2024-02-29, ueber drei 28er-Clamps zurueck auf 2028-02-29).
- [x] **TS-JSON-Cast widens `interval` auf `string`** (2026-06-08, Review; gefixt 2026-06-09, Claude) — `assertInterval`-Narrowing-Helper in [recurrence-vectors.test.ts](src/lib/recurrence-vectors.test.ts) prueft jedes Vektor-`interval` gegen die Whitelist `["monthly", "quarterly", "yearly"]` (`satisfies readonly Interval[]`-Anker am Whitelist-Slice). Tippfehler wie `"Monthly"` fallen jetzt direkt im TS-Test mit Vektor-Name auf, statt erst Rust-seitig.

## 🏛️ Architektur (Diskussion)

User hat am 2026-06-05 explizit eine Architektur-Diskussion angestoßen, weil eine zweite Meinung (Codex) "alles perfekt, keine Luft nach oben" gemeldet hatte. **Diskussion durchgeführt, Ranking nach Wert-pro-Aufwand festgelegt** — Reihenfolge der Hands-on-Umsetzung: (1) Custom Hooks → (3) Reminder im Rust → (2) Tauri-Commands. Punkte 4–8 sind weiterhin Diskussions-Material und werden vor Hands-on einzeln durchgesprochen.

**Reihenfolge nach Diskussion 2026-06-06 gewechselt** auf ➊→➌→➋ (statt ➊→➋→➌). Grund: ➋ braucht DB-Zugriff aus Rust, der ohne ➌ nur als Zweit-Pool neben `tauri-plugin-sql` ginge → Concurrency-Risiko und Code-für-die-Tonne, weil ➌ ihn später ohnehin überholt. Mit ➌ zuerst wird ➋ zum Tagesausflug.

- [x] **➊ Custom Hooks für `App.tsx`-Orchestrierung** (2026-06-05, Commit `1e936ba`) — drei Hooks unter `src/hooks/`: `useSubscriptions` (subs/accounts/loading/error/reload), `useNotificationPermission` (Status + activate), `useReminderLoop` (setInterval-Setup, JSDoc verweist auf ➋). App.tsx von 211 → 152 Zeilen, 7 useState + 3 useEffect → 2 useState + 1 useEffect (UI-State only) + 3 Hook-Aufrufe. Bewusst keine State-Library — Custom Hooks reichen für den Stand, ebnen ➍ (Komponenten-Testbarkeit).
- [x] **➌ Tauri-Commands statt direktem `plugin-sql`-Zugriff** (2026-06-06) — komplett. Tauri-Commands in `src-tauri/src/commands.rs` fuer Subscriptions, Accounts, Reminder-Status und Test-Notification; der fruehere oeffentliche `insert_reminder_if_new`-Command wurde am 2026-06-08 wieder entfernt, weil Reminder-Rows nur vom Rust-Scheduler nach erfolgreichem Notification-Start geschrieben werden sollen. Eigener `sqlx 0.9.0`-Pool im Tauri-State (`AppState { db: SqlitePool }`), Migrations via `sqlx::migrate!("./migrations")`. `tauri-plugin-sql`-Crate aus `Cargo.toml` raus, `@tauri-apps/plugin-sql` aus `package.json` raus, `sql:*`-Permissions aus `capabilities/default.json` raus, `getDb` aus `db.ts` raus. Manuelle Type-Spiegelung via `#[serde(rename_all = "camelCase")]` reichte fuer die Calls; `tauri-specta` blieb ungenutzt — wenn die Code-Basis wächst, kann das nachgerüstet werden.
- [x] **➋ Reminder-Loop ins Rust-Backend** (2026-06-06) — komplett. `src-tauri/src/recurrence.rs` mit anker-additiver `next_due_date`-Logik (5 Tests grün, inkl. der kritische 31.-Drift-Beweis), `src-tauri/src/reminders.rs` mit `run_reminder_check`. `tauri::async_runtime::spawn` im Setup-Block, initial-Check + `tokio::time::sleep(REMINDER_INTERVAL)`-Loop (1h Default). Frontend-Hook `useReminderLoop` und `src/lib/reminders.ts` ersatzlos gelöscht; `App.tsx` rufen das nicht mehr direkt. Das alte Webview-Pause-Problem (Loop hörte bei minimiertem Fenster auf) ist damit weg.
- [x] **➍ Komponenten-Testbarkeit** (2026-06-07, Commit `8761c3d`) — RTL-Setup komplett. `jsdom` als Default-Environment in `vitest.config.ts`, `@vitejs/plugin-react` für JSX-Transform in `.tsx`-Tests, `vitest.setup.ts` lädt `@testing-library/jest-dom`-Matcher und ruft `cleanup()` nach jedem Test (RTL 16 koppelt das an globales `afterEach`, das ohne `globals: true` nicht da ist). `tsconfig.json` um `vitest.setup.ts` erweitert, damit Type-Augmentation der jest-dom-Matcher für `tsc --noEmit` sichtbar wird. Erste Tests als Pattern-Etablierung: `useNotificationPermission.test.tsx` (5 Tests, `renderHook` + `vi.mock("@tauri-apps/plugin-notification")` als Vorlage für Tauri-Plugin-Mocks) und `NotificationPermissionBanner.test.tsx` (4 Tests, `render` + `screen` + `fireEvent.click`). Stand: **36 Tests in 5 Files grün**. Weitere Components/Hooks können dem etablierten Muster folgen.
- [x] **Concerns-Mix in `reminders.rs`** (2026-06-08, Claude) — Aufgeteilt in `compute_due_reminders(subs, today)` (pure, ohne DB/Tauri) + `dispatch_due_reminders(pool, app, granted, due)` (Side-Effects: Idempotenz-Check + Notification + INSERT) + `run_reminder_check` (Orchestrator). 7 neue Unit-Tests fuer den pure-Pfad: Notify-Filter, Lead-Window-Grenzen (today < remind_from), Anker-Drift (31.-Anker bleibt im Maerz auf dem 31.), bad-anchor-date-Fehler, gemischte Batches. Damit ist die fachlich heikle Lead-Window-Logik direkt testbar, ohne DB-Setup oder Tauri-Mock.
- [ ] **Reload-Pattern grobgranular**: jedes CRUD → `reloadAll()` → kompletter DB-Refresh. Bei <50 Subs unbemerkt, danach spürbar. Pattern: optimistic update + lokale State-Mutation + Rollback bei DB-Fehler. Optional, nicht akut.
- [x] **Rust-Commands serverseitig validieren** (2026-06-08, Claude) — Neues Modul [validation.rs](src-tauri/src/validation.rs) mit reinen Helpern (`validate_name/currency/interval/anchor_date/amount_cents/lead_days/min_buffer_cents`) plus async `validate_account_exists(db, id)` (SQLite-FKs sind in der App nicht aktiviert, deshalb expliziter Existence-Check). Zwei Komposit-Helfer `validate_subscription_fields` / `validate_account_fields` werden in `add_subscription`, `update_subscription`, `add_account`, `update_account` aufgerufen. Currency-Whitelist matcht Frontend (`EUR/USD/GBP/CHF/KRW`), Interval-Whitelist matcht das Schema, `lead_days` 0..=365 (mirror Frontend), Anchor-Date strikt `YYYY-MM-DD` (chrono toleriert `2026-6-8` sonst — wuerde der Recurrence-Pipeline schaden). 9 neue Unit-Tests im Modul + Tests bestehen weiterhin alle.
- [x] **TS/Rust-Recurrence-Duplikation mit gemeinsamen Testvektoren absichern** (2026-06-08, Claude) — Neue Datei [tests/fixtures/recurrence-vectors.json](tests/fixtures/recurrence-vectors.json) im Repo-Root als Single Source of Truth fuer 13 `next_due_date`-Vektoren (Anker-in-Zukunft, Drift-Beweis fuer 31.-Anker ueber Februar/Maerz/April/Mai/Juli, quartal/jaehrlich, Schaltjahr-Anker 2024-02-29). Rust-Seite liest sie via `include_str!` + serde_json im neuen `shared_vectors_match_typescript_impl`-Test (`src-tauri/src/recurrence.rs`); TS-Seite via JSON-Import im neuen [recurrence-vectors.test.ts](src/lib/recurrence-vectors.test.ts). Drift faellt damit beim ersten Test-Lauf auf, statt erst in Produktion. Neue Intervalle oder Regeln: einmal in der JSON ergaenzen, beide Seiten implementieren — Test-Lauf zeigt sofort, wenn eine Seite hinterher hinkt.
- [x] **Error-Boundary** (2026-06-06) — `src/components/ErrorBoundary.tsx` als Class-Component (React liefert kein Hook-Equivalent für `componentDidCatch`/`getDerivedStateFromError`). In `main.tsx` um `<App />` gewickelt, fängt damit auch Render-Errors im allerersten App-Mount ab. Fallback-UI nutzt die normale `.container`-Klasse für Layout-Konsistenz, zeigt die Error-Message in `.error-details` (monospace, zart rot), plus Reload-Button (`window.location.reload()`). Beruhigender Hinweis „Daten sind sicher — lokale Datenbank" entspricht der SubTracked-Anti-SaaS-Linie. `componentDidCatch` loggt mit Tag `[ErrorBoundary]` + `componentStack` in die Konsole, direkt im Dev-Build sichtbar.
- [ ] **i18n-Vorbereitung** — **Entscheidung 2026-06-07: A (Status Quo halten)**. Deutsche Strings bleiben hart im JSX, keine `react-i18next`-Infra einziehen. Begründung: SubTracked ist aktuell für Eigenbedarf + deutsches Lernumfeld, deutsche UI ist konsistent zur Doku, vorzeitige i18n-Infra wäre Über-Abstraktion. Re-Evaluieren wenn konkreter Trigger auftaucht — englischsprachiger Nutzer-Anfrage, OSS-Release-Plan, oder Bedarf an zweiter Sprache. Verkoppelt mit „Projektsprache auf Englisch umstellen" in der Später-Sektion; beide Items bleiben offen, aber bewusst nicht jetzt.
- [x] **Dokumentations-Strategie klären** (2026-06-07) — Entscheidung: **Status Quo halten** für alle drei Ebenen. (1) Code-Kommentare: sparsam, nur an besonderen Stellen (JSDoc auf `useReminderLoop`, Inline-Notes bei nicht-offensichtlichen Tricks). Der Grundsatz „kein Kommentar, wenn der Identifier es schon sagt" greift, das *Warum* trägt HANDOVER.md. (2) ADRs: HANDOVER.md erfüllt die ADR-Rolle de facto (Entscheidung + Begründung pro Session); separate ADR-Dateien wären Doppelarbeit zum HANDOVER. (3) App-Logs / strukturiertes Logging: aktuell `eprintln!`/`println!` in Rust nur bei `pnpm tauri dev` sichtbar (Terminal-Output), beim installierten Tauri-Binary verloren — als eigenes Quick-Win-Item ausgelagert (siehe „Strukturiertes Logging im installierten Build" in der Später-Sektion).
- [x] **`PRAGMA foreign_keys=ON` statt validate_account_exists-Patches** (2026-06-08, Review; geklaert 2026-06-09, Claude) — **Annahme war falsch**: sqlx 0.9 aktiviert SQLite-FK per Default (`SqliteConnectOptions::foreign_keys: true`), die App hat also bereits FK-Enforcement. Aufgefallen beim Schreiben des Orphan-DB-Tests im Tests-Block 2026-06-09 (siehe HANDOVER-Eintrag dort). Was tatsaechlich gemacht wurde: **(a)** `foreign_keys(true)` in [lib.rs](src-tauri/src/lib.rs) jetzt explizit gesetzt, damit ein kuenftiger sqlx-Default-Wechsel nicht stillschweigend Production-Verhalten aendert, und damit der `update_subscription_in_db`-Pfad eine dokumentierte Abhaengigkeit hat. **(b)** Doc-Kommentar auf `validate_account_exists` korrigiert — die Funktion bleibt sinnvoll, weil sie einen lesbaren deutschen Fehler liefert (`Konto mit ID 9999 existiert nicht.`) statt SQLites roher FK-Constraint-Meldung. Die ursprueglich vorgeschlagene Entfernung von `validate_account_exists` waere also eine Verschlechterung der UX gewesen.
- [x] **Lock-Poisoning auf `ReminderState.last_check_at` silently dropped** (2026-06-08, Review; gefixt 2026-06-09, Claude) — Neue Methoden `ReminderState::record_check(when)` und `ReminderState::last_check()` in [db.rs](src-tauri/src/db.rs) heilen Poisoning automatisch: `tracing::error!` + `clear_poison()` + Inner-Wert weiternehmen. Das Mutex-Feld ist jetzt privat, beide Call-Sites (Reminder-Loop in `lib.rs`, `get_reminder_status` in `commands.rs`) gehen ueber diese Methoden. Damit friert ein kuenftiger Panik-Hold den Settings-Timestamp nicht mehr permanent ein.
- [x] **Permission-denied tracing::info-Flut im Reminder-Loop** (2026-06-08, Review; gefixt 2026-06-09, Claude) — `dispatch_due_reminders` in [reminders.rs](src-tauri/src/reminders.rs) zaehlt jetzt skipped-no-permission pro Tick und loggt nach der Schleife einmal aggregiert (`count = N`), statt pro faelliger Erinnerung eine separate Info-Zeile zu schreiben. Bei dauerhaft abgelehnter Permission und N Abos: 1 Log/Stunde statt N Logs/Stunde.
- [x] **`pub fn compute_due_reminders` nur in-modul genutzt** (2026-06-08, Review; gefixt 2026-06-09, Claude) — Sichtbarkeit von `compute_due_reminders`, `DueReminder` und `Notifier`-Trait in [reminders.rs](src-tauri/src/reminders.rs) komplett auf modul-privat (kein `pub`) gezogen. Reichweite hatte sich seit Architektur ➋ vom Modul nicht entfernt; der Tests-Block 2026-06-09 hatte zusaetzlich `Notifier` versehentlich `pub` gemacht. Externe Caller koennen jetzt nicht mehr versehentlich am Idempotenz-Check (lebt im Dispatcher) vorbei-Notifications schicken. Tests im `#[cfg(test)] mod tests` sehen die Items weiter via `super::*`.
- [x] **`validate_interval` dupliziert `months_per_interval`-Liste** (2026-06-08, Review; gefixt 2026-06-09, Claude) — Single Source of Truth jetzt in [recurrence.rs](src-tauri/src/recurrence.rs) als `pub const ALLOWED_INTERVALS: &[(&str, u32)]` (Name + Monatsschritt). `months_per_interval` macht einen Slice-Scan darueber und liefert eine Fehlermeldung mit der vollstaendigen Erlaubt-Liste. `validate_interval` in [validation.rs](src-tauri/src/validation.rs) delegiert reine `months_per_interval(interval).map(|_| ())`. Neues Intervall = eine Zeile im Slice; Compiler hilft nicht direkt, aber Schema-CHECK + Test `interval_whitelist_matches_schema` (jetzt iteriert ueber `crate::recurrence::ALLOWED_INTERVALS`) fangen Diskrepanzen ab.
- [x] **`ALLOWED_CURRENCIES` dupliziert frontend `CURRENCY_OPTIONS`** (2026-06-08, Review; gefixt 2026-06-09, Claude) — Single Source of Truth jetzt in [tests/fixtures/currencies.json](tests/fixtures/currencies.json) (Code + Subdivisions pro Waehrung). Rust-Seite via neues Modul [src-tauri/src/currencies.rs](src-tauri/src/currencies.rs) (LazyLock + `include_str!`, Public-API `is_allowed`/`allowed_codes`/`subdivisor`); `validate_currency` und der Notification-Formatierer nutzen diese Helper. TS-Seite importiert dieselbe JSON in [src/lib/format.ts](src/lib/format.ts) und derived `CURRENCY_OPTIONS` + `CURRENCY_SUBDIVISIONS` davon. Beim Hinzufuegen einer neuen Waehrung reicht jetzt ein Eintrag in der JSON; beide Seiten ziehen sofort nach.

## 🌱 Später

- [ ] **UI-Redesign Richtung arsnova.eu** (Angular Material 3 / Material You als Vorlage) — Teal-Akzentpalette, Card-Layout, Material Icons, vollständige Dark-Mode-Parität, App-Feel statt Web-Formular. Vor Umsetzung Lib-Auswahl (MUI v6 / Material Web / shadcn+Tokens / Tailwind+M3). Designziel und nicht-zu-übernehmende Teile siehe Serena-Memory `mem:ui_vision`
- [ ] **Settings-Dialog ausbauen.** Mögliche sinnvolle Einträge: App-Version, Log-Ordner öffnen/kopieren, Test-Notification, Update-Check, Dark/Light-Preference falls später eigener Theme-Toggle kommt.
- [ ] **Buttons und Aktionen stärker app-artig gestalten.** Langfristig im UI-Redesign: Icon+Text-Buttons für primäre Aktionen, kompaktere Row-Actions statt reine Textbuttons, klare destruktive Aktion für Löschen/Archivieren.
- [ ] **Empty-State nützlicher machen.** Statt nur "Noch keine Abos angelegt": direkter CTA "Neues Abo", optional "Konto anlegen", kurzer Hinweis auf den Nutzen. Nicht als Marketing-Hero, sondern als arbeitsfähiger Erstzustand.
- [ ] App-Icon / Branding
- [ ] Import/Export (CSV) der Abos
- [ ] **Import/Export als Backup-Format erweitern.** CSV reicht für Tabellen-Kompatibilität; JSON wäre besser für vollständiges Backup inklusive Konten, Reminder-Metadaten, Archivstatus und später Preis-Historie.
- [ ] **Variable Intervalle.** Alle 2 Monate, halbjährlich, wöchentlich/alle X Wochen. Vor Umsetzung Recurrence-Testvektoren für TS + Rust anlegen, damit die 31.-Anker-Invariante nicht bricht.
- [ ] **Kündigungsfrist / "kündigen bis"-Datum.** Pro Abo optional Kündigungsfrist und automatische Anzeige, bis wann reagiert werden muss. Passt gut zum Abo-Tracker-Charakter, ist aber nicht Kern des Kontodeckungs-MVP.
- [ ] **Kategorien/Tags für Abos.** Streaming, Versicherung, Hosting, Mobilfunk, Finanzen etc. Hilft später bei Auswertungen und Filterung.
- [x] **Südkoreanischer Won (KRW)** (2026-06-06) — komplett: KRW in `CURRENCY_OPTIONS`, neue `CURRENCY_SUBDIVISIONS`-Map und `getCurrencySubdivisor()`-Helper in [format.ts](src/lib/format.ts) (EUR/USD/GBP/CHF → 100, KRW → 1, Default 100), `formatAmount` nutzt den Helper. Im SubscriptionDialog ist das Betrags-Input conditional: bei Subdivisor 1 (KRW) `step="1"` + `min="1"` und keine Decimal-Stellen im `centsToInput`, sonst wie gehabt `step="0.01"`. Speicherung in DB: `Math.round(amountNumber * subdivisor)` — `amount_cents` ist semantisch „smallest currency unit", bei KRW direkt Won, bei EUR Cent. Visuell verifiziert (Anlegen, Liste, Edit-Roundtrip, EUR-Sanity-Check). Vitest-Spec deckt KRW-Formatierung.
- [ ] Optionale weitere Kanäle (z.B. Telegram) als Alternative zu nativen Notifications
- [x] Waisen-Reminder beim Löschen eines Abos verhindern (2026-06-06) — gelöst in der Application-Logik des neuen `delete_subscription`-Commands (Architektur ➌): Transaktion löscht zuerst alle Reminders des Abos, dann das Abo selbst. Schöne Konsequenz: der FK-Constraint, der vorher schweigend im Schema schlief und unter dem strikteren sqlx-Pool plötzlich aktiv wurde, wird jetzt sauber bedient — ohne Schema-Rebuild via `ON DELETE CASCADE` (der unter sqlx::migrate! durch die Auto-Transaction-Semantik mit `PRAGMA foreign_keys=OFF` umständlich gewesen wäre).
- [x] `SubRow.interval`-Cast (2026-06-05) — `SubRow.interval: string` (wie aus SQLite kommt), neuer privater `parseInterval(s)`-Helper in `db.ts` validiert und narrowed beim Mapping, wirft bei unbekanntem Wert. Defense-in-Depth gegen DB-Manipulation von außen.
- [x] `tauri-plugin-opener` entfernt (2026-06-05) — war seit Tauri-Template-Setup eingebunden, im Frontend nirgends genutzt. Vier Stellen weg (Cargo.toml/Lock, lib.rs, capabilities/default.json, package.json/Lock). `cargo check` + `pnpm install` clean.
- [x] **Lokalisierung der Eingaben** (2026-06-07) — `parseAmountInput(input)`-Helper in [format.ts](src/lib/format.ts) akzeptiert `12,99`, `12.99`, `1.234,56` (DE-Tausender), `1,234.56` (EN-Tausender), `1.234.567,89` etc. Heuristik: bei beiden Trennzeichen ist das spaeter stehende der Dezimaltrenner; bei genau einem mit 3 Stellen danach (z.B. `1,234`) wird er als Tausender gedeutet. `SubscriptionDialog` von `<input type="number">` auf `<input type="text" inputMode="decimal">` umgestellt — `type=number` blockierte in WebKitGTK je nach Locale die Komma-Eingabe stumm. `centsToInput` formatiert Edit-Vorbefuellung jetzt mit Komma. 10 Helper-Tests in `format.test.ts` + 3 Dialog-Tests fuer Komma/Tausender/ungueltige Eingabe.
- [x] **Komponenten-Tests via React Testing Library** (2026-06-07) — Setup erledigt im Rahmen von Architektur ➍ (Commit `8761c3d`). Erste Smoke-Tests für `useNotificationPermission` und `NotificationPermissionBanner` als Vorlage. SubscriptionDialog/SettingsDialog können dem etablierten Muster folgen, wenn sie touched werden.
- [x] **Strukturiertes Logging im installierten Build** (2026-06-07) — umgesetzt mit `tracing` + `tracing-subscriber` + `tracing-appender`: stdout im Dev-Modus plus Daily-Rolling-File im `app_log_dir()` (Linux: `~/.local/share/com.tcgtvv.subtracked/logs/`, max. 7 Tagesdateien). `WorkerGuard` wird via `app.manage()` gehalten. Reminder-Loop loggt Fehler via `tracing::error!`. Review 2026-06-07 (Codex): Backlog-Status nachträglich korrigiert.
- [x] **`CoverageItem` um `subscriptionId` erweitern** (2026-06-06) — neues Feld `subscriptionId: number` im Interface, in `computeCoverage` als `sub.id` gefüllt, in `OverviewSection.tsx` als React-Key (`${it.subscriptionId}-${it.date}`) genutzt. Damit eindeutiger Key egal ob zwei Subs identisch heißen und am gleichen Tag fällig wären (z.B. zwei Netflix-Profile auf demselben Konto). camelCase wegen Frontend-Konvention.
- [ ] **Projektsprache auf Englisch umstellen.** Bisher überall Deutsch (UI-Strings, Code-Kommentare, BACKLOG/HANDOVER, Commit-Messages). Open-Source-Standard ist Englisch — größere Reichweite, Community-Anschluss, kompatibel mit Standard-Tooling und Code-Hosting-Konventionen. Pragmatisch zusammen mit dem i18n-Refactor angehen (siehe Architektur-Diskussion), sonst doppelte Arbeit am gleichen Material.
- [ ] **Backlog auf Industriestandard umschreiben.** Aktuell themen-basierte Aufgabenlisten (Bugs, Jetzt, Distribution, Architektur, …). Stattdessen Format näher an Industriestandard: User Stories ("Als X will ich Y, damit Z"), Akzeptanzkriterien, ggf. Story Points / Priorisierung. Lerneffekt für reale Team-Workflows. Sinnvoll erst wenn das Projekt eine gewisse Reife hat — vorher unverhältnismäßiger Overhead für ein Solo-Projekt.

## Hinweise

Konventionen und Stack siehe [AGENTS.md](./AGENTS.md). Geld in Cent, Datum als `YYYY-MM-DD`, `recurrence.ts` nicht naiv umschreiben.
